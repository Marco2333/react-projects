import ArticalList, {stateKey, initialState} from './view';
import reducer from './reducer';
import * as actions from './actions';

export {
    reducer,
    stateKey,
    initialState,
    actions
};

export default ArticalList;